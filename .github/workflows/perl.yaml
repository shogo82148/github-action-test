name: perl

on:
  workflow_dispatch:

jobs:
  setup-perl:
    runs-on: "windows-latest"
    steps:
      - uses: shogo82148/actions-setup-perl@debug
        with:
          distribution: strawberry
          perl-version: "5.34"
